# GlobalTech World Computer Training Institute

A Computer Training institute for training students and professinals tech related courses.  
Built with **HTML5**, **CSS**, **PHP**, and **MySQL**.

## 📂 Folder Structure
- `assets/` → Styles, scripts, and media files  
- `includes/` → Shared PHP files (DB connection, header, footer)  
- `pages/` → All main pages (home, login, register, etc.)  
- `database/` → SQL export file  
- `uploads/` → Uploaded images or user files  

## ⚙️ Setup Instructions
1. Import `database/globaltech_db.sql` into MySQL via phpMyAdmin  
2. Update DB credentials in `includes/db_connect.php`  
3. Run the project in XAMPP (put folder inside `htdocs`)  
4. Visit in browser: `http://localhost/globaltech-elearning`

